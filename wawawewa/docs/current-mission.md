# Current mission: Single Product page — "Futuristic v2" direction

Temporary scratch file — delete once this mission is fully done (see CLAUDE.md "Current mission tracking"). The prior "Homepage Futuristic redesign" mission's code is complete (every strip built, see `docs/checklist.md` section 5) — its one remaining item, filling in real content in wp-admin, is now tracked directly in `docs/checklist.md` instead of here, so nothing was lost by starting fresh.

## Goal
Build the WooCommerce single product page per `Product Page Futuristic v2.dc.html` (project `6e15106a-a49d-45e9-85cb-b7e220f17f7e`), reusing real WooCommerce data/functionality everywhere possible rather than hand-rolling anything WooCommerce already provides.

## Decisions made
- **Variation UI**: style WooCommerce's own native variations form (handles price/stock/image swaps, AJAX add-to-cart automatically) rather than hand-building the mockup's custom color-circle/size-pill swatches with custom JS bridging to WC's variation system. Not pixel-identical, but zero custom sync logic to get wrong. User confirmed via AskUserQuestion.
- **Gallery**: the theme already declares `wc-product-gallery-zoom`/`-lightbox`/`-slider` support (`inc/woocommerce.php`) — WooCommerce's own default gallery template + JS (flexslider, zoom, photoswipe lightbox) already does everything the mockup's main-image-plus-4-thumbnails layout needs. Only restyling via SCSS, no template override, no custom gallery JS — a functional bonus over the mockup (real zoom/lightbox it doesn't show).
- **"◆ 360°" badge**: skipped — it implies an actual interactive 360° product viewer, which isn't being built. Showing the badge without the feature would be misleading.
- **Related products' shine-sweep + "quick view" hover overlay**: skipped — no quick-view modal is being built, and a shine effect with no purpose is pure decoration for decoration's sake. Reused the same tilt-hover treatment already established for the homepage Best-sellers cards instead, for a consistent interaction language.
- **Star rating**: unlike the homepage Testimonials strip (which hardcodes 5 stars because its mockup data has no real rating field), this is genuine WooCommerce review data — rendered as real filled/empty stars from `$product->get_average_rating()` rounded to the nearest whole number, with the real `get_review_count()`. Section is skipped entirely if there are no reviews yet.
- **Accordions** (Description / Shipping & Returns / Care): extracted the homepage FAQ strip's accordion styling out of the homepage-scoped `.strip-faq` into a new shared `sass/components/_accordion.scss` (generic `.accordion` BEM classes), since it's now used in two places — avoids duplicating the same CSS. `js/faq-accordion.js` needed no changes; it already targets generic `[data-faq-accordion]`/`[data-faq-toggle]` attributes, not FAQ-specific classes.
  - "תיאור מלא" (Full description): real `$product->get_description()`.
  - "משלוח והחזרות" (Shipping & returns): **global** ACF option field (`product_shipping_returns_text`, new "מוצר" tab on site settings) — same policy text across the whole catalog, not per-product.
  - "מידות וטיפוח" (Sizing & care): **per-product** ACF field on the `product` post type (`product_care_instructions`) — care instructions genuinely vary by product/material. Accordion row is skipped if empty.
- **Trust badges row** (free shipping / 30-day returns / secure payment): global ACF option fields (`product_trust_badge_1/_2/_3`, same "מוצר" tab) — same 3 lines under every product, not worth re-entering per product.
- **Related products**: reused WooCommerce's own related-products query (real "same category/tag" algorithm) via overriding `woocommerce/single-product/related.php` for card styling only — not a custom repeater. Updated `woocommerce_output_related_products_args` filter (`inc/woocommerce.php`) from 3 cols/3 posts to 4 cols/4 posts to match the design.
- **Breadcrumb**: reused WooCommerce's own `woocommerce_breadcrumb()` (already fires the correct hook/filter) restyled via CSS, instead of hand-rolling HOME/CATEGORY/PRODUCT text.

## Steps
- [x] ACF: new "מוצר" (Product) tab on site settings options page (`product_trust_badge_1/_2/_3` text with mockup-matching defaults, `product_shipping_returns_text` textarea with a mockup-matching default) — `acf-json/group_wawawewa_site_settings.json`
- [x] ACF: new field group on the `product` post type (`product_care_instructions` textarea, skipped if empty) — `acf-json/group_wawawewa_product_fields.json`
- [x] `woocommerce/content-single-product.php` override — full custom layout calling real `woocommerce_template_single_*()` functions (title/rating/price/excerpt/add-to-cart) in our own two-column grid, not WC's default stacked hook order. Stock badge (real `is_in_stock()`), primary category link (`get_the_terms`), particle canvas, trust badges, accordions all built here.
- [x] `woocommerce/single-product/rating.php` override — plain ★/☆ + review count matching the design, from real `get_average_rating()`/`get_review_count()`; returns early if reviews are disabled or there are zero ratings
- [x] `woocommerce/single-product/related.php` override — real WC related-products query (`$related_products`/`$args` passed in by `woocommerce_output_related_products()`), restyled with the shared `.product-card` component, `[data-tilt]` hover
- [x] `sass/components/_accordion.scss` — extracted shared accordion styles out of `.strip-faq`; `template-parts/strips/faq.php` now uses the generic `.accordion`/`.accordion__item` etc. classes; `js/faq-accordion.js` needed no changes (already generic)
- [x] `sass/components/_product-card.scss` — same extraction for the product card, out of `.strip-best-sellers__card`; `template-parts/strips/best-sellers.php` now uses `.product-card` classes; reused directly in `related.php` above
- [x] `sass/utilities/_particles.scss` — same extraction for the particle-canvas CSS, out of the homepage-scoped `.homepage-particles`; renamed to generic `.particles-canvas`, `page-templates/home-page.php` updated to match, reused in `content-single-product.php`
- [x] `sass/plugins/woocommerce/_single-product.scss` — full rewrite: breadcrumb (styling WC's own `woocommerce_breadcrumb()`, already fired by the existing `woocommerce_before_main_content` hook — no new call needed), gallery restyle (targets WC's/flexslider's real class names: `.woocommerce-product-gallery__wrapper`, `.flex-viewport`, `.flex-control-thumbs`, `.flex-active`), summary column, native `.variations`/`<select>` styling, `.quantity`/`.single_add_to_cart_button` styling, trust badges, related-products grid
- [x] `inc/woocommerce.php` — bumped related-products args to 4 cols/4 posts
- [x] `inc/setup-functions/enqueue.php` — `particles`/`product-tilt`/`faq-accordion` now also enqueue on `is_product()` (previously homepage-only); added `product-quantity-stepper` (product-page only)
- [x] `js/product-quantity-stepper.js` (new, not originally planned) — progressive-enhancement +/- buttons around WooCommerce's native `.quantity` `input.qty`, reading its real min/max/step attributes rather than assuming values. Chosen as a safe middle ground: the mockup's stepper UI is easy to add without touching cart/variation logic at all (unlike the color/size swatches, which were rejected for exactly that reason).
- [x] **Build tooling fix (unplanned, requested mid-mission)**: merged the separate `sass/woocommerce.scss` compile entry into `sass/style.scss` — WooCommerce styling (`sass/plugins/woocommerce/*`) now compiles through the same single stylesheet as everything else, so saving `style.scss` (or any partial it imports) refreshes all compiled CSS instead of needing to remember to also save the separate `woocommerce.scss` entry point. Deleted `sass/woocommerce.scss` and the stale compiled `woocommerce.css` at the theme root; moved its 3 color variables (`$woocommerce__color-error/-success/-info`) into `sass/abstracts/variables/_colors.scss`. Updated `inc/woocommerce.php`'s `wawawewa_woocommerce_scripts()` to stop enqueuing the now-deleted `woocommerce.css` — **found and fixed a real hook-order bug while doing this**: its inline `@font-face` (WooCommerce's own star icon-font, still needed for the *default* `.star-rating` markup on shop-loop/review/widget pages we haven't overridden — our custom `rating.php` override just doesn't need it for the single-product page specifically) was re-attached to the `'wawawewa-style'` handle, but `inc/woocommerce.php` is `require`d *before* `inc/setup-functions/enqueue.php` in `functions.php`, and both hook `wp_enqueue_scripts` at the same default priority — so without bumping this hook to priority 20, it would have tried to attach inline CSS to a handle that isn't registered yet and silently no-op'd (the exact same class of bug CLAUDE.md already documents for the header.php RTL-swap case). Updated `CLAUDE.md`'s Stack/Directory-structure/Conventions sections and `docs/store-build-plan.md` to stop referencing the now-deleted `woocommerce.scss`/`woocommerce.css`.
- [x] **Bug fix (unplanned, found on staging)**: a default WordPress sidebar (Recent Posts/Comments/Archives/Categories, plus a search box) was rendering at the very bottom of the product page. Root cause: WooCommerce's own `templates/single-product.php` fires `do_action( 'woocommerce_sidebar' )` right after the main content, before the footer — nothing in this theme's own code was hooked there, but something evidently was. Fixed with `remove_all_actions( 'woocommerce_sidebar' )` in `inc/woocommerce.php`, since this design has no sidebar concept anywhere (every homepage strip and the product page are full-width) — whatever renders there should never show, regardless of what it turns out to be.
- [x] Mobile pass against `Product Page Futuristic Mobile v2.dc.html` — gallery main image 340px (was a rough 320px guess before this fetch), thumbnail grid gap 8px/height 64px/radius 10px, stock badge/category/title/price/excerpt/rating font sizes all matched exactly to the mockup's mobile values, quantity stepper + add-to-cart shrunk to 44px height, variations `<select>` goes full-width instead of capped at 260px. **Fixed a real mismatch**: trust badges were set to stack into a single column on mobile (a guess made before this mockup existed) — the actual mobile design keeps all 3 in a row, just with a tighter gap/padding/font; corrected to match.
- [x] **Bug fix (unplanned, found on staging)**: the FAQ accordion (and the product page's accordion) stopped opening/closing entirely. Root cause: when the accordion was extracted into the shared `.accordion` component, the markup class was renamed from `.strip-faq__item` to `.accordion__item`, but `js/faq-accordion.js` was never updated to match — `button.closest('.strip-faq__item')` returned `null`, throwing inside the click handler and silently killing the whole toggle. Fixed by updating both references in `js/faq-accordion.js` to `.accordion__item`.
- [x] **Two bugs found on staging after a real add-to-cart test**, both in `sass/plugins/woocommerce/_components.scss`:
  1. The "added to cart" success notice (`.woocommerce-message`) showed as a flat, unstyled green banner — leftover `_s` starter-theme boilerplate (`background-color: $woocommerce__color-success`, no border-radius/padding/typography) never restyled for this design. Rebuilt as a soft tint + border + rounded box matching the site (same treatment for `.woocommerce-info`/`.woocommerce-error`), and the "view cart" link inside already inherits the shared `.button` pill styling since WooCommerce renders it with class `button wc-forward`.
  2. The sale price showed with an ugly solid-yellow highlight box — WooCommerce wraps the new/sale price in `<ins>`, but the theme's base typography (`sass/base/typography/_copy.scss`) applies a yellow highlight background to *every* `<ins>` site-wide (for blog "inserted text"). Reset specifically within `.price`/`.product-card__price` context so it doesn't leak into price displays (covers the single-product page and the Best-sellers/related product cards).
- [x] **"Added to cart" notice turned into an auto-dismissing toast** (requested after seeing the restyled banner): `.woocommerce-message` is now a small fixed top-center box (`sass/plugins/woocommerce/_components.scss`) that fades in, holds ~1s, then fades itself out via `js/wc-notice-toast.js` (enqueued site-wide, since the notice can appear after redirect from any page, not just the product page). Error/info notices were deliberately left as regular static banners — those need to stay until the shopper reads/acts on them. **RTL note**: centering uses physical `left: 50%` (with `/*rtl:ignore*/`), not logical `inset-inline-start`, because the `left:50%` + `translateX(-50%)` trick only cancels out on the same physical axis — using the logical property here would've anchored the opposite edge in RTL and pushed the toast off-center; this is a case where physical positioning is correct because dead-center is direction-independent (same category as `.alignleft`/`.alignright` in CLAUDE.md's RTL section).
- [x] **Sale badge moved + designed** (found on staging, another unstyled default): WooCommerce's `.onsale` "Sale!" flash was rendering as plain black text above the gallery image — it's hooked to `woocommerce_before_single_product_summary` priority 10, right before the gallery, with zero styling now that the default WC stylesheet is disabled. Removed that one hook attachment (`remove_action(..., 'woocommerce_show_product_sale_flash', 10)` in `inc/woocommerce.php` — scoped to the single-product hook only, the shop-loop/archive hook is untouched) and added a custom-designed `single-product__sale-badge` (solid gold pill, real `$product->is_on_sale()` check) directly above the stock badge in `content-single-product.php`, per request.
- [ ] Verify on staging once a real product exists — RTL first, then desktop, then mobile. **The gallery styling in particular could not be verified without a live WordPress install** — flexslider's thumbnail-nav markup is generated by WooCommerce's own JS at runtime, and the CSS here targets its documented class names defensively rather than against an inspected live DOM; check this first on staging.

## Notes
- Both desktop (`Product Page Futuristic v2.dc.html`) and mobile (`Product Page Futuristic Mobile.dc.html`, if/when it exists — not yet fetched) should be pulled together per the established per-section convention; only the desktop v2 file was provided so far.
- Full design spec (colors, spacing) copied from the fetched mockup — see this session's fetch of `Product Page Futuristic v2.dc.html` if a re-read is ever needed; not re-transcribed here since implementation follows immediately.
